services:
  backend:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    env_file:
      - .env
    volumes:
      - ..:/workspaces:cached
    depends_on:
      database:
        condition: service_healthy
    command: sleep infinity
