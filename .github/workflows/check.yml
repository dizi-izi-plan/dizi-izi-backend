name: Check
on:
  workflow_call:
  workflow_dispatch:
  push:
    branches-ignore:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  flake8:
    name: Running flake8 check for code
    runs-on: ubuntu-22.04
    steps:
      - name: Flake8 Code Check
        uses: dizi-izi-plan/dizi-izi-backend/.github/actions/flake8@ci-cd-pipelines

      - run: echo "The name of the docker image is ${{ github.repository }}"
